nonterminal_symbols:
  # ? name
  ? S
  ? A
  ? B
  ? C
  ? D
terminal_symbols:
  # name: regex
  a: a
  b: b
ignored_symbols:
start_symbol: S
production_rules:
  # - left part -> right part
  - S -> C b B A
  - A -> A a b
  - A -> a b
  - B -> C
  - B -> D b
  - C -> a
  - D -> a